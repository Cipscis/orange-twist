import {
	describe,
	test,
} from '@jest/globals';

describe('loadDays', () => {
	test.todo('returns a Promise that resolves when the days register has been filled with the persisted days data');
});
