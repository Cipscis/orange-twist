import {
	describe,
	test,
} from '@jest/globals';

describe('deleteDayInfo', () => {
	test.todo('when passed a day name without any day data, does nothing');

	test.todo('when passed a day name that has day data, removes that day from the register');
});
