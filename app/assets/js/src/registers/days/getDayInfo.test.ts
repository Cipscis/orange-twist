import {
	describe,
	expect,
	test,
} from '@jest/globals';

import { getDayInfo } from './getDayInfo';

describe('getDayInfo', () => {
	test.todo('when passed no arguments, returns an array of info on all days');

	test.todo('when passed a day name that has no matching day, returns null');

	test.todo('when passed a day name that has a matching day, returns that day\'s info');
});
