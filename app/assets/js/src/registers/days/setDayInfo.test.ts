import {
	describe,
	test,
} from '@jest/globals';

describe('setDayInfo', () => {
	test.todo('when passed an invalid day name, throws an error');

	test.todo('when passed a day name without existing data, creates a new day with default information filling in the blanks');

	test.todo('when passed a day name with existing data, updates that day with the passed data');
});
