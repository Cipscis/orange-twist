import {
	describe,
	test,
} from '@jest/globals';

describe('startAnimationLoop', () => {
	test.todo('calls the callback each frame, with the time passed');

	test.todo('skips the callback if called twice for the same frame');

	test.todo('stops when the signal is aborted');

	test.todo('does nothing if passed an aborted signal');
});
